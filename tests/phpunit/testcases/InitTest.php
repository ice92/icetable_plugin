<?php
namespace Inc;
use \Inc\Init;
use \Brain\Monkey\Functions;
use \Mockery;
class InitTest extends \PluginTestCase {
    public function test_services() {
        // $class = Mockery::mock('class');
        // $Init = new Init();
        // $this->assertEquals(new $class,$Init->instantiate($class));
        $this->assertTrue(true);
    }
}